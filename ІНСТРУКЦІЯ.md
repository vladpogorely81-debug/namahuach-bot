# 🤖 Бот для ставок ЧС 2026 — Інструкція запуску

## Що потрібно (все безкоштовно)
- Акаунт Telegram
- Акаунт GitHub (github.com)
- Акаунт Railway (railway.app)

---

## КРОК 1 — Створи бота в Telegram

1. Відкрий Telegram, знайди `@BotFather`
2. Напиши `/newbot`
3. Придумай назву бота (наприклад: `ЧС 2026 Ставки`)
4. Придумай username (наприклад: `wc2026_bets_bot`) — має закінчуватись на `bot`
5. BotFather дасть тобі **токен** — збережи його, він виглядає так:
   ```
   7123456789:AAHxxx-xxxxxxxxxxxxxxxxxxxxxxxxx
   ```

---

## КРОК 2 — Дізнайся свій Telegram ID (для прав адміна)

1. Напиши боту `@userinfobot` в Telegram
2. Він дасть твій числовий ID, наприклад: `123456789`

---

## КРОК 3 — Завантаж код на GitHub

1. Зайди на **github.com**, увійди або зареєструйся
2. Натисни **New repository** (зелена кнопка вгорі)
3. Назви репозиторій: `wc2026-bot`
4. Зроби його **Private**, натисни **Create repository**
5. Завантаж всі файли з папки `wc2026_bot`:
   - Натисни **uploading an existing file**
   - Перетягни всі файли (bot.py, db.py, scoring.py, matches_data.py, requirements.txt, railway.toml, .gitignore)
   - Натисни **Commit changes**

---

## КРОК 4 — Задеплой на Railway

1. Зайди на **railway.app**
2. Натисни **Login** → **Login with GitHub**
3. Натисни **New Project** → **Deploy from GitHub repo**
4. Вибери `wc2026-bot`
5. Railway почне деплой — зачекай 1-2 хвилини

### Додай змінні середовища (це важливо!)

У Railway знайди свій проект → вкладка **Variables** → **Add Variable**:

| Назва | Значення |
|-------|----------|
| `BOT_TOKEN` | токен від BotFather (з кроку 1) |
| `ADMIN_IDS` | твій Telegram ID (з кроку 2) |

Якщо адмінів кілька — через кому: `123456789,987654321`

6. Після додавання змінних Railway автоматично перезапустить бота

---

## КРОК 5 — Перевір що все працює

1. Знайди свого бота в Telegram за username
2. Напиши `/start`
3. Повинно прийти привітання ✅

---

## Команди бота

### Для всіх учасників:
| Команда | Що робить |
|---------|-----------|
| `/start` | Реєстрація та головне меню |
| `/calendar` | Календар найближчих матчів |
| `/bet` | Зробити ставку на матч |
| `/balance` | Мій баланс та статистика |
| `/standings` | Таблиця всіх учасників |
| `/history` | Моя історія ставок |
| `/rules` | Правила гри |

### Для адміна (тільки ти):
| Команда | Приклад |
|---------|---------|
| `/admin` | Список всіх команд адміна |
| `/matches` | Всі матчі та їх статус |
| `/result <id> <г1> <г2>` | `/result 5 2 1` — матч #5 завершився 2:1 |
| `/addmatch Команда1 \| Команда2 \| стадія \| дата` | `/addmatch Бразилія \| Аргентина \| sf \| 07.07` |
| `/broadcast текст` | Розіслати повідомлення всім |
| `/bank` | Загальна сума в банку |

### Стадії для /addmatch:
- `group` — Груповий етап (10 грн)
- `r32` — 1/32 (20 грн)
- `r16` — 1/16 (30 грн)
- `qf` — 1/4 (40 грн)
- `sf` — 1/2 (50 грн)
- `final` — Фінал (100 грн)

---

## Як провести чемпіонат

1. **Поділися посиланням** на бота з учасниками
2. **Всі пишуть** `/start` та роблять ставки через `/bet`
3. **Після кожного матчу** ти вводиш результат: `/result 1 2 1`
   - Бот автоматично рахує всіх, хто скільки виграв/програв
   - Оновлює баланси
4. **Таблицю** в будь-який момент можна переглянути через `/standings`

---

## Питання?

Якщо щось не запустилось — перевір:
- Чи правильно скопійований `BOT_TOKEN` (без пробілів)
- Чи правильний `ADMIN_IDS` (тільки цифри)
- Логи в Railway (вкладка **Deployments** → клікни на деплой → **View Logs**)
